import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.santiago.oldskull',
  appName: 'Oldskull Companion',
  webDir: 'dist', //vite build output directory
}

export default config
